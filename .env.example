# Firebase Configuration (Required)
NEXT_PUBLIC_FIREBASE_API_KEY="your-firebase-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-firebase-auth-domain"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-firebase-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-firebase-storage-bucket"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-firebase-messaging-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-firebase-app-id"
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="your-firebase-measurement-id"

# Firebase Admin (Required for server-side operations)
FIREBASE_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project-id","private_key_id":"your-private-key-id","private_key":"your-private-key","client_email":"your-client-email","client_id":"your-client-id","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"your-cert-url"}'
FIREBASE_PROJECT_ID="your-firebase-project-id"
FIREBASE_CLIENT_EMAIL="your-firebase-client-email"
FIREBASE_PRIVATE_KEY="your-firebase-private-key"

# OpenAI Configuration (Required for AI features)
OPENAI_API_KEY="your-openai-api-key"

# Google Maps Configuration (Required for location features)
# IMPORTANT: Use separate API keys for Google Maps, NOT the same as Firebase API key
# This prevents REQUEST_DENIED and ApiTargetBlockedMapError issues
GOOGLE_MAPS_API_KEY="your-google-maps-api-key"
GOOGLE_MAPS_SERVER_API_KEY="your-google-maps-server-api-key"
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-client-api-key"

# Google Maps API Setup Instructions:
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create a new project or select existing project
# 3. Enable the following APIs:
#    - Maps JavaScript API
#    - Places API (New) - NOT the legacy Places API
#    - Geocoding API
#    - Distance Matrix API
# 4. Create API credentials:
#    - Client-side key: Restrict to HTTP referrers (your domain)
#    - Server-side key: Restrict to IP addresses (your server IPs)
# 5. Set up billing (required for Places API)
# 6. Test your API keys before deployment

# Stripe Configuration (Required for payments)
STRIPE_SECRET_KEY="your-stripe-secret-key"
STRIPE_WEBHOOK_SECRET="your-stripe-webhook-secret"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="your-stripe-publishable-key"

# Auth Configuration (Required for authentication)
NEXTAUTH_URL="http://localhost:3000"
GOOGLE_CLIENT_ID="your-google-oauth-client-id"
GOOGLE_CLIENT_SECRET="your-google-oauth-client-secret"

# Email Configuration (Required for email features)
EMAIL_SERVER="your-email-server"
EMAIL_SERVER_HOST="your-email-host"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="your-email-user"
EMAIL_SERVER_PASSWORD="your-email-password"
EMAIL_FROM="noreply@example.com"

# Application Configuration
NEXT_PUBLIC_WEBSITE_URL="http://localhost:3000"
AGE_VERIFICATION_EXPIRY_DAYS="30"
NEXT_PUBLIC_ENABLE_ANALYTICS="false"
NEXT_PUBLIC_MAINTENANCE_MODE="false"
NEXT_PUBLIC_WEBSOCKET_URL="ws://localhost:3001"
NEXT_PUBLIC_AMPLITUDE_API_KEY="your-amplitude-api-key"

# Development
NODE_ENV="development"
